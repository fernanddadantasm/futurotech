import os 
os.system("cls")

# lista_numeros = list(range(1, 102, 2 ))
# print(lista_numeros)

# Gerando uma lista de números pares de 0 a 200
# lista_numeros = list(range( 201, 0, -1))
# print(lista_numeros)


# range é uma função independente, pode ser usada separada do for
